name: Check cml comment publishing
on:
  pull_request:
permissions:
  id-token: write
  contents: write
  pull-requests: write
jobs:
  setup:
    environment: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npx github:iterative/cml#d001-watermark-spaces
      - name: comment
        run: |
          ./comment.sh "First comment"
          cml comment create --update report.md
